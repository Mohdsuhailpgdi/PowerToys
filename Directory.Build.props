<Project>
  <PropertyGroup>
    <!-- Force SDK that is guaranteed on GitHub runners -->
    <WindowsTargetPlatformVersion>10.0.22621.0</WindowsTargetPlatformVersion>

    <!-- Ensure NuGet + CsWinRT resolve native/runtime assets -->
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
  </PropertyGroup>
</Project>
