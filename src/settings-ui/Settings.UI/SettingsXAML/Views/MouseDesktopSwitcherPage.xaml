<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.MouseDesktopSwitcherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:viewmodels="using:Microsoft.PowerToys.Settings.UI.ViewModels"
    mc:Ignorable="d">

    <controls:SettingsPageControl ModuleImageSource="ms-appx:///Assets/Modules/MouseDesktopSwitcher.png">
        <controls:SettingsGroup x:Uid="MouseDesktopSwitcher_Group_Enable">
            <controls:SettingsCard x:Uid="MouseDesktopSwitcher_Enable_Card" Header="Enable Mouse Desktop Switcher">
                <ToggleSwitch IsOn="{x:Bind ViewModel.Enabled, Mode=TwoWay}" />
            </controls:SettingsCard>
        </controls:SettingsGroup>
        
        <controls:SettingsGroup x:Uid="MouseDesktopSwitcher_Group_Behavior" Header="Behavior">
            <controls:SettingsCard x:Uid="MouseDesktopSwitcher_Sensitivity_Card" Header="Sensitivity">
                <Slider Value="{x:Bind ViewModel.Sensitivity, Mode=TwoWay}" Minimum="0" Maximum="100" />
            </controls:SettingsCard>
            <controls:SettingsCard x:Uid="MouseDesktopSwitcher_Debounce_Card" Header="Debounce (ms)">
                <NumberBox Value="{x:Bind ViewModel.DebounceMs, Mode=TwoWay}" Minimum="0" Maximum="2000" />
            </controls:SettingsCard>
        </controls:SettingsGroup>
    </controls:SettingsPageControl>
</Page>
